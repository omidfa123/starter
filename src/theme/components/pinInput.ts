import type { ComponentStyleConfig } from '@chakra-ui/theme';

export const PinInput: ComponentStyleConfig = {
  variants: {
    flushed: {
      bg: 'white',
      h: '64px',
      w: '42px',
      rounded: 8,
      fontSize: 18,
      gap: 0,
    },
  },
};
